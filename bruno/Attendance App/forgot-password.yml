info:
  name: forgot-password
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5000/api/auth/forgot-password
  body:
    type: json
    data: |-
      {
        "email": "iankithinji19@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
