info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |
      {
        "email": "iankithinji19@gmail.com",
        "password": "#kim8606"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
