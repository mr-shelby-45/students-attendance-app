info:
  name: reset-password
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5000/api/auth/reset-password
  body:
    type: json
    data: |
      {
        "email": "iankithinji19@gmail.com",
        "otp": "521948",
        "newPassword": "#kim8606"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
